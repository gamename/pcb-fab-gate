"""pcb-gate: arming assertions, canary injection, antenna-keepout geometry,
and an independent overlap check for the SVW PCB fab gate (SVW-0034).

Checker design authored under GNI-0283, carried forward unchanged by SVW-0036
into this public, cross-org repo so every board org can call the same gate.
"""

__version__ = "0.1.0"
